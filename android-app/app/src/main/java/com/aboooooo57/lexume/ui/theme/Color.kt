package com.aboooooo57.lexume.ui.theme

import androidx.compose.ui.graphics.Color

// Placeholder Material 3 tonal palette (M1) - not the app's real brand
// colors yet. Reading themes (system/light/dark/sepia, the analog of
// `ReadingTheme.swift`) are a separate, reader-scoped concept that arrives
// with M5 and intentionally don't live here - this file is just the app
// chrome's Material palette.
val LexumePrimaryLight = Color(0xFF2E5A45)
val LexumePrimaryDark = Color(0xFF9AD2B5)
val LexumeSecondaryLight = Color(0xFF4C6358)
val LexumeSecondaryDark = Color(0xFFB2CCC0)
