package com.aboooooo57.lexume.ui.theme

import androidx.compose.material3.Typography

// Default Material 3 type scale for now (M1) - the reader's own font
// family/size controls (serif/mono/sans, matching `fontFamilyKey`/
// `fontSizeKey` in `AppSettings.swift`) are a separate, per-paragraph
// concern that arrives with M5, not part of this app-wide type scale.
val LexumeTypography = Typography()
